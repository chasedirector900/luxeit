import { SectionSkeleton } from "@/components/layout/section-skeleton";

// Branch boundary: fronts /explore and its deep product/review pages during
// cross-tab navigation — neutral by design (see SectionSkeleton).
export default function ExploreLoading() {
  return <SectionSkeleton />;
}
